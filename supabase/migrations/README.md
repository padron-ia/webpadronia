# Migraciones

Las migraciones se aplican contra el proyecto Supabase `tfhmeoiryhuivdnpwpjs` y quedan
registradas en su historial. Aquí se guarda una copia para que el repo no vaya a ciegas.

⚠️ **`../schema.sql` es la foto de abril de 2026 y está desfasado**: no contiene ninguna de
las cuatro vistas del portal (`client_invoices`, `client_invoice_lines`, `client_proposals`,
`expiration_alerts`) ni las migraciones posteriores. Para saber qué hay de verdad en la base,
mirar el proyecto, no este fichero.
